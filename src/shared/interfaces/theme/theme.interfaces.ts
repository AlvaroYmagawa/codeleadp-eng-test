export type IColors = {
  primary: string;
  background: string;
  element: string;
  primaryText: string;
  strongText: string;
  opaqueText: string;
};

export interface ITheme {
  colors: IColors;
}
